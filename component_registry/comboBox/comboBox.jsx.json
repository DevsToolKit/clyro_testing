{
  "$schema": "https://clyro.devstoolkit.space/schema/registry-item.json",
  "name": "comboBox",
  "type": "registry:ui",
  "author": "clyro (https://clyro.devstoolkit.space)",
  "dependencies": [
    "@radix-ui/react-slot"
  ],
  "files": [
    {
      "path": "ui/comboBox.jsx",
      "content": "\"use client\";\n/* -------------------- Imports -------------------- */\n// External libs, UI components, and utilities\nimport * as React from \"react\";\nimport { Select, SelectTrigger, SelectContent, SelectItem, SelectValue, } from \"./select\";\nimport { Input } from \"./input\";\nimport { cn } from \"@/lib/utils\";\nimport { SearchIcon } from \"lucide-react\";\nimport { Separator } from \"./separator\";\n/* -------------------- Component -------------------- */\n// ComboBox with searchable dropdown functionality\nfunction ComboBox({ items, value, onValueChange, placeholder = \"Select an item\", errorMessage, noMatchFallback = \"No such item found\", className, }) {\n    const [query, setQuery] = React.useState(\"\");\n    const filteredItems = items.filter((item) => item.toLowerCase().includes(query.toLowerCase()));\n    return (React.createElement(\"div\", { className: cn(\"space-y-1\", className) },\n        React.createElement(Select, { value: value, onValueChange: onValueChange },\n            React.createElement(SelectTrigger, null,\n                React.createElement(SelectValue, { placeholder: placeholder })),\n            React.createElement(SelectContent, { className: \"pt-0\" },\n                React.createElement(\"div\", { className: \"py-2 sticky top-0 z-[1] bg-background\" },\n                    React.createElement(Input, { icon: React.createElement(SearchIcon, { className: \"w-4 h-4\" }), type: \"text\", value: query, onChange: (e) => setQuery(e.target.value), placeholder: \"Search...\", className: \"h-8 text-sm\" })),\n                React.createElement(Separator, null),\n                filteredItems.length > 0 ? (filteredItems.map((item) => (React.createElement(SelectItem, { key: item, value: item }, item)))) : (React.createElement(\"div\", { className: \"pt-2 py-2 text-sm text-muted-foreground\" }, noMatchFallback)))),\n        errorMessage && (React.createElement(\"p\", { className: \"text-sm text-destructive pt-1\" }, errorMessage))));\n}\n/* -------------------- Exports -------------------- */\n// Exporting the ComboBox component\nexport { ComboBox };\n",
      "type": "registry:ui",
      "target": ""
    }
  ]
}